rec fun fact (n) {
    if n = 1 then {1}
    else {
        n * fact (n - 1)
    }
}

rec fun fib (n) {
    if n = 1 then {1}
    else {
    if n = 2 then {1}
    else {
         fib (n - 1) + fib (n - 2)
    }
    }
}


print(fact(10))

print(fib(10))
